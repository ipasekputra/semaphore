---
- name: Bootstrap Python for Ansible
  hosts: web_servers
  gather_facts: no
  become: true

  tasks:
    - name: Update apt
      ansible.builtin.raw: apt update -y

    - name: Install python3-six
      ansible.builtin.raw: apt install -y python3-six
